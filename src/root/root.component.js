// @flow

// Dependencies
import * as React from 'react'

// Components
import Routes from './root.routes'

// Main Component
type Props = {
  store: Object
}

const Root = ({store}: Props) => <Routes />

export default Root
